
let mod = angular.module('userProfileControllerModule', []);

mod.controller('userProfileController', [
  function() {
    /**
     * This uses the Controller As syntax. Anything needed in the view should be placed on `ctrl` and is accessible
     * in the view as `userctrl`;
     */
    const ctrl = this;



    angular.extend(ctrl, {
    });
}]);

export default mod = mod.name;
